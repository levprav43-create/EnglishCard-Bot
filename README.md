# EnglishCard — Telegram-бот для изучения английского

Курсовая работа по курсу «Базы данных» в Нетологии.

## 🗃️ Схема базы данных

Проект использует **3 таблицы**:

### 1. `users`
- `user_id` (BIGINT, PK) — ID пользователя в Telegram
- `username`, `first_name`, `last_name` — данные пользователя
- `created_at` — дата регистрации

### 2. `words`
- `word_id` (SERIAL, PK) — ID слова
- `russian_word` — русское слово
- `english_word` — английский перевод

### 3. `user_words`
- `id` (SERIAL, PK)
- `user_id` (FK → `users`)
- `word_id` (FK → `words`)
- `added_at` — когда слово добавлено

> Все пользователи получают общие слова при первом запуске.  
> Удаление работает только для текущего пользователя.